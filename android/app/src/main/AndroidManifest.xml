<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is needed only to reach a benchmark backend the user points the
      app at. Every screen ships inside the APK and renders with no network at
      all, so the app is fully usable offline and denying it costs only live
      data.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Reminders are delivered by the phone (ADR-031): AlarmManager holds each
      one and a notification shows the learner's words at its time.
      POST_NOTIFICATIONS is asked for at runtime on Android 13+, only when the
      learner is setting reminders. RECEIVE_BOOT_COMPLETED puts the alarms
      back after a restart, which Android otherwise drops. No exact-alarm
      permission: an inexact alarm may run a few minutes late in Doze, which a
      revision reminder can afford.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quintek"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
          The blanket android:usesCleartextTraffic="true" that used to be here
          is gone. It existed so a developer could point the app at
          http://192.168.x.x on their LAN, but it applied to every build and
          every destination — so a shipped APK would have sent a learner's
          bearer token and every answer they gave over plaintext HTTP.

          The capability is now SCOPED rather than removed:

            res/xml/network_security_config.xml        cleartext DENIED
            src/debug/res/xml/network_security_config.xml   cleartext ALLOWED

          The debug source set overrides the resource, so LAN development is
          unchanged for `assembleDebug` and a release APK cannot make a
          cleartext request at all, whatever URL is entered in Settings.
        -->

        <!-- The learner's app. Launching goes straight here. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Quintek.NoActionBar"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Long-press the icon -> "Benchmark console". -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:parentActivityName=".MainActivity" />

        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".ReminderBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
