[pytest]
testpaths = tests
markers =
    browser: drives a real Chromium through Playwright. Slower (~2 minutes) and
        needs a browser on disk, so it is separable: `-m "not browser"` runs the
        rest. It is NOT optional in CI -- what a pricing page renders when the
        backend is down cannot be checked by parsing HTML.
